<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền của ảnh -->
    <solid android:color="@android:color/transparent" />

    <!-- Bo tròn các góc -->
    <corners android:radius="20dp" />

    <!-- Độ dày viền -->
    <stroke
        android:width="2dp"
        android:color="#D32F2F" /> <!-- Màu của viền -->
</shape>